# =============================================================================
# DAILY BRIEF CONFIGURATION
# =============================================================================
# To enable/disable data sources, edit config.ts and set enabled: true/false
# =============================================================================

# -----------------------------------------------------------------------------
# Jira Configuration
# -----------------------------------------------------------------------------
# Authentication: Personal Access Token (PAT)
# 1. Go to Jira > Profile > Personal Access Tokens
# 2. Click "Create token"
# 3. Give it a name and set expiration
# 4. Copy the token immediately (you won't see it again)
# Required permissions: Read access to issues
# -----------------------------------------------------------------------------
JIRA_BASE_URL=***
JIRA_USERNAME=***
JIRA_PAT=***

# -----------------------------------------------------------------------------
# GitLab Configuration
# -----------------------------------------------------------------------------
# Authentication: Personal Access Token (PAT)
# 1. Go to GitLab > User Settings > Access Tokens
# 2. Click "Add new token"
# 3. Select scopes: read_api, read_user, read_repository
# 4. Set expiration date and create
# 5. Copy the token immediately (you won't see it again)
# -----------------------------------------------------------------------------
GITLAB_BASE_URL=***
GITLAB_PAT=***

# -----------------------------------------------------------------------------
# Google Drive Configuration (OAuth2)
# -----------------------------------------------------------------------------
# Authentication: OAuth 2.0 with Service Account or User Credentials
#
# Setup Steps:
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Create a new project or select existing
# 3. Enable the Google Drive API:
#    - APIs & Services > Library > Search "Google Drive API" > Enable
# 4. Create OAuth 2.0 credentials:
#    - APIs & Services > Credentials > Create Credentials > OAuth client ID
#    - Application type: Desktop app (or Web application)
#    - Download the JSON or copy Client ID and Client Secret
# 5. Configure OAuth consent screen if prompted
# 6. Run: npm run gdrive:auth
#    - This will open a browser for authorization
#    - Grant access to your Google Drive
#    - The refresh token will be printed to console
# 7. Get the Folder ID from the Google Drive URL:
#    - Open the folder in Google Drive
#    - Copy the ID from the URL: drive.google.com/drive/folders/<FOLDER_ID>
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=***
GOOGLE_CLIENT_SECRET=***
GOOGLE_REFRESH_TOKEN=***
GOOGLE_DRIVE_FOLDER_ID=***

# -----------------------------------------------------------------------------
# Email Configuration (SMTP)
# -----------------------------------------------------------------------------
# Authentication: SMTP credentials from your email provider
#
# Common SMTP settings:
# - Gmail: smtp.gmail.com, port 587 (use App Password, not regular password)
# - Outlook: smtp.office365.com, port 587
# - Internal: Check with your IT department
#
# For Gmail App Passwords:
# 1. Enable 2-Factor Authentication on your Google Account
# 2. Go to Google Account > Security > App passwords
# 3. Generate a new app password for "Mail"
# 4. Use that 16-character password as SMTP_PASS
# -----------------------------------------------------------------------------
SMTP_HOST=***
SMTP_PORT=25
SMTP_USER=***
SMTP_PASS=***
EMAIL_RECIPIENT=***
EMAIL_FROM=***
