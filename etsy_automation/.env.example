# Etsy login (required unless using existing browser tab)
ETSY_EMAIL=your@email.com
ETSY_PASSWORD=your_password

# Use an existing browser tab where you've already signed in (no login in script)
# 1. Launch Chrome with: chromium --remote-debugging-port=9222
#    (or: "C:\...\Chrome.exe" --remote-debugging-port=9222 on Windows)
# 2. Sign in to Etsy in that browser and open Shop Manager / orders.
# 3. Set USE_EXISTING_BROWSER=true and run the script.
USE_EXISTING_BROWSER=true
BROWSER_CDP_URL=http://localhost:9222

# Message to send: either MESSAGE_TEXT or MESSAGE_FILE (path relative to this folder)
MESSAGE_TEXT=Hi! Thanks for your order. We'll ship soon.
# MESSAGE_FILE=message.txt

# Identifies this "campaign" so we never send the same message twice to the same order.
# Change this when you send a different message.
MESSAGE_KEY=thank_you_2025

# Optional: base URL for orders list (if you don't provide order_url in orders.csv)
ORDERS_BASE_URL=https://www.etsy.com/your/orders/sold/open

# Random delay between each message (milliseconds). Reduces rate-limit risk.
DELAY_MIN_MS=8000
DELAY_MAX_MS=22000

# Set to false to watch the browser (debugging)
# HEADLESS=false
