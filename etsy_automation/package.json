{
  "name": "etsy-automation",
  "version": "1.0.0",
  "description": "Browser automation to send Etsy messages to order buyers with CSV tracking and resume support",
  "type": "module",
  "main": "send-message.js",
  "scripts": {
    "send": "node send-message.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.49.0"
  }
}
